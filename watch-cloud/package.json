{
  "name": "lustre-watch",
  "private": true,
  "workspaces": ["apps/*", "services/*", "packages/*"],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "dev": "npm run dev -w @lustre/watch-web",
    "test": "npm run test -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present"
  },
  "engines": { "node": ">=22" }
}

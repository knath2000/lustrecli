import { decodeBase64, normalizePairingCode, requireProtocolVersion, validateDisplayName } from "@/lib/cloud/device-contract";
import { publicKeyThumbprint, secretHash } from "@/lib/cloud/device-crypto";
import { beginEnrollment } from "@/lib/cloud/device-repository";
import { jsonError, requestBody } from "@/lib/cloud/route";
export async function POST(request: Request) { try { const body = await requestBody(request); requireProtocolVersion(body.protocolVersion); const point = decodeBase64(body.publicKey, 65, "publicKey"); if (point.length !== 65 || point[0] !== 4) throw new Error("Invalid public key."); const platform = body.platform === "macos" ? "macos" : null; if (!platform || typeof body.agentVersion !== "string" || body.agentVersion.length < 1 || body.agentVersion.length > 80) throw new Error("Invalid device metadata."); const enrollment = await beginEnrollment({ pairingCodeHash: secretHash(normalizePairingCode(String(body.pairingCode))), publicKey: String(body.publicKey), keyThumbprint: publicKeyThumbprint(point), displayName: validateDisplayName(body.displayName), platform, agentVersion: body.agentVersion }); return Response.json({ protocolVersion: 1, enrollmentID: enrollment.id, nonce: enrollment.nonce, expiresAt: enrollment.expiresAt.toISOString() }, { headers: { "Cache-Control": "no-store" } }); } catch (error) { return jsonError(error); } }
